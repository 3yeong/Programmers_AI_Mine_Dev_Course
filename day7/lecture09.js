// var a = 5 , b = 3;

// a + b;

// a - b;

// a%b // 나머지

// -a 단항 연산자 

//  ++a; --a //증감연산자 
//변수의 앞과 뒤에 모두 쓸 수 있는데 결과값이 조금 다른 수 있다.

Math.pow  (a,b) // a의 b제곱 
Math.sqrt () // 루트
Math.random() // 0~1사이 랜덤 숫자

var a = 1;
console.log(a);
console.log(--a); //a = a + 1
console.log(a);